import { LuxonDateFormatPipePipe } from './luxon-date-format-pipe.pipe';

describe('LuxonDateFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LuxonDateFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
